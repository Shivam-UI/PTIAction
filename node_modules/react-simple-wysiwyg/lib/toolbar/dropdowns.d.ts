import { HTMLAttributes } from 'react';
import { EditorState } from '../editor/EditorContext';
export declare const BtnStyles: typeof Dropdown;
export declare function Dropdown({ items, selected, ...inputProps }: DropdownProps): JSX.Element;
export declare type DropDownItem = [
    string,
    string | ((editorState: EditorState) => void),
    string
];
export interface DropdownProps extends HTMLAttributes<HTMLSelectElement> {
    selected?: number;
    items?: DropDownItem[];
}
